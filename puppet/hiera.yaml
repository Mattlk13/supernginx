---
:hierarchy:
  - "%{::hostname}"
  - "locations/%{::location}"
  - "environments/%{::envname}"
  - "profiles/%{::profile}"
  - domains
  - common
:backends:
  - yaml
:yaml:
  :datadir: '/vagrant/puppet/hieradata'
